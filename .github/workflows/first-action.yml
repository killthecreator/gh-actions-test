name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo 'First step'
      - name: last step
        run: echo 'Last step'
